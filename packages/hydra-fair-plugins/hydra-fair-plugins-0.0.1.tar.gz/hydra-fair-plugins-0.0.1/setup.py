from setuptools import setup

setup(
    name = 'hydra-fair-plugins',
    version = '0.0.1',
    description = 'This is only a placeholder package.',
)

raise RuntimeError("This is a dummy package that was never ment to be actually installed from public PyPI. Please double check your installation instructions.")
