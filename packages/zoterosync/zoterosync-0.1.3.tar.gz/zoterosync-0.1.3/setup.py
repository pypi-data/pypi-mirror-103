#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['zoterosync']

package_data = \
{'': ['*']}

install_requires = \
['click', 'pybtex>=0.24.0', 'python-dotenv>=0.15.0', 'python-rclone>=0.0.2']

entry_points = \
{'console_scripts': ['zoterosync = zoterosync.zoterosync_cli:main']}

setup(name='zoterosync',
      version='0.1.3',
      description='it syncs zotero storage with a rclone remote',
      author='Marcelo Akira Inuzuka',
      author_email='marceloakira@ufg.br',
      url='https://gitlab.com/marceloakira/zoterosync',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3',
     )
