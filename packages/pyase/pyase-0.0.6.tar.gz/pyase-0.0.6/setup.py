
from setuptools import setup

setup(
    name='pyase',
    version='0.0.6',
    description='Bindings for .ASE and .ASEXE (not yet) files.',
    long_description='I will add this later, dont worry. Just note: package is called ase.',
    packages=["ase"]
    )

