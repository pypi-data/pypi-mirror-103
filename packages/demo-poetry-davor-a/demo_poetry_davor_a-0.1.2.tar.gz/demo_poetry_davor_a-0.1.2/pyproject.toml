[tool.poetry]
name = "demo_poetry_davor_a"
version = "0.1.2"
description = ""
authors = ["Jorge <larajorge11@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.9"
loguru = "*"
psutil = "*"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
run = "main:script"
