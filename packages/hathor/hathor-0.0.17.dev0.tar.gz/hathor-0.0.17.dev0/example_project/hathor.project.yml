name: Example Project

metadata:
  author: BrinkerVII
  author_email: brinkervii@gmail.com

build:
  directory: ./build
  builder: modular

  profiles:
    dist:
      directory: ./dist
      builder: single

    sdist:
      directory: ./dist
      builder: zip_archive

    backup:
      directory: /tmp/backup
      builder: copy

serve:
  host: 127.0.0.1
  port: 8080
  build_profile: default

sources:
  src:
    enabled: true

save_instance_root:
  ./src/Instances
