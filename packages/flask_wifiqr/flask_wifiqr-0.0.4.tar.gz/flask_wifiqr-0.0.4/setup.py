#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['flask_wifiqr']

package_data = \
{'': ['*'], 'flask_wifiqr': ['templates/*']}

install_requires = \
['Flask', 'Flask-QRcode']

extras_require = \
{'test': ['pytest', 'pytest-cov', 'pyproject-flake8']}

setup(name='flask_wifiqr',
      version='0.0.4',
      description='Flask_WifiQR: A simple flask app that serves your wifi password as a QR Code to smartphones',
      author='Flip Hess',
      author_email='flip@fliphess.com',
      url='https://github.com/fliphess/wifiqr',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      python_requires='>=3.7',
     )
